package components;

/**
 * contains the different types of tokens that form a line statement
 */
public enum TypeToken {Label, Mnemonic, OperandOffset, OperandLabel, StringOperand, Directive, Comment, EOL, EOF}